log 1: Started with building the basic pipeline, using the hugging face api and got results from LLM. We used gemini 3 flash 1.2T parameters.
log 2: Started working on mapping the LLM results with evalution script. plus worked on different JSON output, i.e. simple JSON and Nested JSON, but simple worked better.
log 3: Decided which platform/models and its family to use in our development, 2 model and parameter were decided at first, i.e. Llama 3.1 8b/70b and Qwen 2.5 7b/70b
log 4: At start we choosed pydantic prompt with JSON string output and then we fetch/map and convert the result into CSV format on first 20 game summaries.
log 5: Initialized the dimensions: D1- model, D2- model parameters, D3- Prompt strategies, D4- data structure (JSON). Prompt strategies includes: Regular/normal prompt, Act as Judge. Also we switched from 20 to 30 game summaries.
log 6: Started with different prompt strategies from (p0-p3) with decided dimension (log 5) to get number of combinations, to get better and more results to compare with these combinations. prompts: p0: normal prompt, p1: tuned prompt (with rules), p2: JSON format for p1, p3: LLM as judge.
log 7: Small models were runned on local Ollama and Medium models were runned on HPC clusters.
log 8: For now (log 6) is temporary development pipeline, Now we shifted to evalution script to evalute LLM results and get the all LLM models precision and recall.
log 9: handled the LLM output and made it standarised so it can smoothly run the traditional 2021 evalution script, also solved the bugs in script.
log 10: As LLM models were hallucinating and giving the NaN results, we decided to perform the post processing (before evaluation) on LLM result csv. For first we just cleaned the data by droping the rows with NaN values.
log 11: Rather then just dropping, decided to implement 5 step post processing. 5 steps process includes: A : ID basis (document_only) B: Recovery Strategy: nearest_fixed_n / nearest_with_backoff C: Match Strictness: strict / casefold / normalized D: Overlap Resolver: If overlap then resolve left to right E: Unmatched policy: If repair still fails after all that, just drop the row.
log 12: After applying the post processing steps, we faced the errors, so we tried different combinations of abcde: ae, abe etc. to solve errors. With these we got many different combinations to compare processing steps. Plus here we get idea about the little twiking and tuning our evalution script to make ready it for the larger models (like Chatgpt, Gemini, Deepseek) in development.   
log 13: As we were facing the same/ similar results, we just start thinking for the another approaches: like Context-Anchored string search and Sequence Alignment/ Dynamic Programming. 
log 14: We didn't recieved that good results from p4: (context based search). Then we start looking at post-processing code, if we could get any changes to make it optimal. Also we found that p0 was giving high precision and low recall (due to LLM generating longer span).   
log 15: After getting the same results and long span answers, we included a new prompt: p0a that basically allow llm to follow strict rules, now we were getting the low span results.  
log 16: We started to implement our approaches on test_set. But we found out the same results as we were getting during development set. Now we are analysing the results and trying to find out the pattern, the prompt and LLM follows.
log 17: Missing model dict entries + 70B/72B removed from Grove, so continuing the development on small model.
log 18: master_comparison analysis: recall is the bottleneck (under-generation), not span position; sentence mode wins; stage C adds nothing. 
log 19: Designed p0b (=p5): hard span limit, minimal correction rule, expanded CONTEXT/NOT_CHECKABLE examples, "find all errors". p0b results: span fix worked strongly (llama mean 1.26 tokens, token_precision 0.242 up from 0.088)
log 20: Qwen finds more errors (recall of qwen 0.150 vs llama 0.089) but with messier spans (token_precision of qwen 0.189 vs llama 0.242). Llama is more precise but more conservative.